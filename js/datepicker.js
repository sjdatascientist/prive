import { Datepicker } from "vanillajs-datepicker";

// const datepicker = document.querySelectorconst elem = document.getElementById('foo');
const elem = document.getElementById('foo');
const datepicker = new Datepicker(elem, {
  // ...options
}); 